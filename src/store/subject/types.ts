import createAsyncActionSet, { AsyncActionSet } from 'store/types';

export const GET_SUBJECTS: AsyncActionSet = createAsyncActionSet('GET_SUBJECTS');
export const ADD_SUBJECT: AsyncActionSet = createAsyncActionSet('ADD_SUBJECT');
